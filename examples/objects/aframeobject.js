import AFrameGroup from '../../src/aframegroup.js';

export default class AFrameObject extends AFrameGroup {
    /**
     * on create scene (or earliest possible opportunity)
     * @param scene
     */
    onCreate() {
    }
}
